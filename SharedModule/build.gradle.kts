// SharedModule - shared library for MWK providers (no standalone plugin)

cloudstream {
    description = "MWK Shared Library - extractors, models, utilities"
    language = "id"
    authors = listOf("MWK")
}
